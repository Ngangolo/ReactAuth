import MasterLayout from "./MasterLayout";

const Dashboard = ({ children }) => {

    return (
        <MasterLayout>
        <div>

            <h1>Welcome Admin!</h1>
        </div>
        </MasterLayout>
    );
}

export default Dashboard;